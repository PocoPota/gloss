# PDF翻訳システム 設計メモ

論文等のPDFを「レイアウトを維持したまま日本語に翻訳」するシステムの設計検討。

## 1. ゴールと要件

- **入力**: 学術論文等のPDF（英語想定、2段組も含む）
- **出力**: 日本語訳されたPDF（または HTML）。原文のレイアウト（図表・段組・見出し構造）を可能な限り保持
- **非目標**: 数式・コード・図表内テキストの翻訳は原則スキップ（誤訳を避けるため）

## 2. アプローチの選択肢

PDFは「紙のための最終形式」なので、構造情報が失われていることが多い。翻訳の難しさの9割はここ。大きく4つのアプローチがある。

### A. テキスト置換方式（原PDFに上書き）
原PDFのテキストブロックを検出 → 日本語に置換 → 元の座標に再描画。

- **Pros**: 見た目がほぼ原文と同じ
- **Cons**: 日英で文長が大きく違う（日本語の方が短いことも長いことも）、フォントサイズ自動調整が難しい、はみ出し・改行制御が地獄
- **代表実装**: [PDFMathTranslate (Byaidu/pdf2zh)](https://github.com/Byaidu/PDFMathTranslate) — これが現状の実質デファクト

### B. 構造抽出 → HTML/Markdown 再生成方式
PDFをMarkdown/HTMLに構造変換 → 翻訳 → 再レンダリング。

- **Pros**: リフローするので文長の差を吸収できる、数式をLaTeX保持可能、編集可能な成果物
- **Cons**: 段組・細かいレイアウトは失われる、見た目は原文と別物になる
- **代表ツール**: [Marker](https://github.com/VikParuchuri/marker), [MinerU](https://github.com/opendatalab/MinerU), [Nougat](https://github.com/facebookresearch/nougat)

### C. バイリンガル並置方式
原文ページの横（または下）に翻訳ページを並べる／段落ごとに原文と訳文を交互に。

- **Pros**: 実装が一番楽、参照しながら読みたい人に便利（学習用途として最適）
- **Cons**: 「レイアウトそのまま」ではない

### D. OCR＋画像オーバーレイ方式
PDFを画像化 → OCR → 翻訳 → 元画像にテキストを重ねる。

- **Pros**: 画像PDF（スキャン論文）にも対応
- **Cons**: テキストPDFに対して情報を捨てる、精度が落ちる

## 3. 推奨アプローチ

**「A（レイアウト保持）」をメインに、B（Markdown変換）を補助出力として両方出す** のが最もニーズを満たす。

> 「レイアウトできるだけそのまま」という要件はAしか満たせない。ただしAは破綻する箇所が必ずあるので、BをフォールバックUIとして併設する。

### 具体的な構成（推奨）

```
[PDF入力]
   ↓
[1] レイアウト解析 (PyMuPDF + layout model)
   ↓ テキストブロック・図表・数式の境界を抽出
[2] 翻訳対象の選別
   - 本文段落 → 翻訳
   - 数式/コード/図中テキスト → 保持
   - 見出し・キャプション → 翻訳
   ↓
[3] 翻訳実行 (LLM または DeepL)
   - 論文脈のコンテキスト（前後段落）を付けて翻訳精度UP
   - 用語一貫性のためグロッサリーを記憶
   ↓
[4a] レイアウト保持PDF生成  ← メイン出力
   - 原文テキストを削除 → 訳文を同座標に日本語フォントで描画
   - 文字はみ出し時はフォント縮小 or 行増加で対応
[4b] Markdown/HTML生成     ← サブ出力（崩れた時用）
```

## 4. 技術選定（候補）

### PDF解析
| ライブラリ | 用途 | 備考 |
|---|---|---|
| **PyMuPDF (fitz)** | テキスト・座標抽出、書き込み | 第一候補。書き込みAPIがある |
| **pdfplumber** | 表抽出に強い | 補助用 |
| **Marker / MinerU** | 論文の構造化 | Bルートで使う |

### レイアウト認識
- **[PP-StructureV3](https://github.com/PaddlePaddle/PaddleOCR)** / **LayoutLMv3** — テキストブロック・図表・数式の領域分類
- 軽量でよければ PyMuPDF の `get_text("dict")` だけでもブロックは取れる

### 数式保護
- **[pix2tex (LaTeX-OCR)](https://github.com/lukas-blecher/LaTeX-OCR)** — 数式画像 → LaTeX、翻訳対象から除外するために位置だけ保護する用途でもOK

### 翻訳エンジン
| 選択肢 | Pros | Cons |
|---|---|---|
| **Claude API (Sonnet 4.6 / Opus 4.7)** | 文脈理解、用語一貫性、技術語強い、プロンプトで挙動制御可 | コスト、レイテンシ |
| **DeepL API** | 速い・安い・自然 | 論文特有の専門用語は弱め、API制約 |
| **GPT-4o / o1** | Claude同等 | 同上 |
| **ローカルLLM（Qwen2.5, ELYZA等）** | 無料・プライバシー | 品質・速度 |

→ **論文なら Claude API 推奨**。プロンプトキャッシュで前段の文脈を使い回せるので長尺でも現実的。

### 日本語フォント埋め込み
- **Noto Sans JP / Noto Serif JP** を PDF に埋め込む（PyMuPDFの`insert_font`）
- 数式・英数字は原フォント維持したほうが見栄えがいい

## 5. 実装の勘所（ハマりポイント）

1. **2段組の読み順問題**: 単純なY座標ソートだと左右混ざる。カラム検出が必須（x座標でクラスタリング or レイアウトモデル）
2. **段落の再結合**: PDFは行ごとに分かれている。文末句読点・インデント・フォント変化で段落境界を推定してから翻訳にかけないと、一文ずつ翻訳されて文脈が消える
3. **ハイフネーション**: 行末の `evolu-\ntion` を `evolution` に戻してから翻訳
4. **参照番号・引用**: `[12]`, `Fig. 3` などは翻訳時に壊れやすい。プレースホルダに置換→翻訳→復元
5. **数式インライン**: `$x^2$` 的なインライン数式を保護
6. **文長膨張/収縮**: 日本語は概ね英語の0.6〜0.9倍の文字数だが、テクニカルな箇所では膨らむ。はみ出し時はフォント微縮小 or 行送り調整
7. **フッター/ヘッダー/ページ番号**: ノイズとして除外
8. **表の扱い**: セル単位で翻訳。構造は原PDF座標を保つ

## 6. MVP（最小実装）の進め方

段階的に作る。いきなり完璧を目指さない。

- **Phase 0**: 英文PDF → 全文抽出 → Claude APIで段落単位翻訳 → Markdown出力（レイアウト無視）
- **Phase 1**: バイリンガル並置PDF出力（原文ページ + 訳文ページ）— C方式
- **Phase 2**: レイアウト保持PDF（A方式）。1段組から
- **Phase 3**: 2段組対応、数式・図表保護
- **Phase 4**: 用語集・過去訳メモリ、UI

Phase 1 までで実用価値は十分出る。Phase 2 以降が本番。

## 7. 参考になる既存実装

設計のときに必ず読む。

- **[PDFMathTranslate (Byaidu/pdf2zh)](https://github.com/Byaidu/PDFMathTranslate)** — 最も近いOSS。内部実装を読むと段組処理・数式保護のやり方が学べる
- **[Marker](https://github.com/VikParuchuri/marker)** — PDF→Markdown変換の品質が高い
- **[MinerU](https://github.com/opendatalab/MinerU)** — Marker競合、中国発で論文への最適化が強い
- **[Nougat](https://github.com/facebookresearch/nougat)** — Meta製、学術PDFに特化したVLM

> 「A方式を一から作る」より、PDFMathTranslate をフォークして日本語翻訳パイプラインに差し替えるのが現実的。

## 8. 次のアクション（提案）

1. サンプルPDF（1本）を決める（翻訳したい実物）
2. まず Marker で Markdown 化して、どれくらい構造が取れるか確認
3. 並行して PyMuPDF で座標付きテキスト抽出を試す
4. Claude API で段落翻訳のプロンプトを詰める（用語一貫性が勘所）
5. Phase 1 の並置PDF → Phase 2 の置換PDF の順に進める

---

まずどのフェーズから着手するか決めたら教えてください。フォントやAPIキーの準備も並行で進められます。
